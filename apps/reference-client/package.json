{
  "name": "@cemp/reference-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Headless two-user CKB testnet reference client proving the full CEMP message lifecycle (ckd.txt §20).",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "client": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@cemp/ckb": "workspace:*",
    "@cemp/core": "workspace:*",
    "@cemp/crypto": "workspace:*",
    "@ckb-ccc/core": "1.12.5"
  }
}
