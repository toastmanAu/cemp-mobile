/**
 * Shared UI package (spec §16). Components and screens land here during
 * Phase 6 (messenger shell). This package may use React Native primitives;
 * packages/cemp-core, cemp-ckb, cemp-crypto, cemp-database and
 * cemp-secure-vault must NOT (AGENTS.md).
 */

export const UI_PACKAGE_PLACEHOLDER = true;
