# tools/devnet

Helpers for running a local CKB dev chain for fast, deterministic integration
tests (block-time control, prefunded accounts, no faucet dependency).

Planned: a docker-less script wrapping `ckb` dev chain + miner, plus fixture
JSON for Alice/Bob identities matching packages/cemp-test-vectors.
Tracked on the project kanban board.
